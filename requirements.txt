torch>=2.1.0
torchaudio>=2.1.0
kokoro>=0.9.4
huggingface-hub>=1.0  # mlx-audio 0.5.4+ requires >=1.0; old <1.0 cap dropped
tokenizers>=0.20
demucs>=4.0.0
gradio>=4.0
pedalboard>=0.9.0
numpy
scipy
soundfile
pyloudnorm
noisereduce
librosa>=0.10.0           # audio utils (resampling, analysis)
pyworld>=0.3.4
python-dotenv
google-genai>=1.16.0
httpx>=0.28.0      # script_gen adapters: openai-compatible (Ollama/OpenRouter) HTTP client
anthropic>=0.89.0  # script_gen adapters: Anthropic API client
mlx
mlx-audio  # DeepFilterNet MLX for neural speech denoising
accelerate>=0.26.0
f5-tts>=1.0.0
silero-vad
# protobuf: onnx 1.21+ (via s3tokenizer/f5-tts) needs builder (added in 3.20).
# google-api-core 2.30+ needs >=4.25.8. descript-audiotools & tensorboard 2.9
# have stale <3.20 constraints but don't use protobuf at runtime.
protobuf>=4.25.8,<7.0.0
